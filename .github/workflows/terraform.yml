name: Terraform
description: Placeholder workflow for now

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  temp: "value"
  
jobs:
  job:
    name: "placeholder"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
